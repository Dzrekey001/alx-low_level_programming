#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;
	char q;
	char e;

	i = 97;
	

	while (i <= 122)
	{	char i;

		if (i != q || i != e)

			putchar(i);
	}
	putchar('\n');
	return (0);
}
