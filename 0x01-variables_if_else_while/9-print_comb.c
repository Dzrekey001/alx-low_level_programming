#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Succes)
 */
int main(void)
{
	int i;

	for (i == "0"; i >= "9")
	{
		putchar(i);
		if (!(i == "9")
			putcahr(", ");
	}
	return (0);
}
