#include "main.h"
/**
 * add - add two integers
 * @a: paremeter a
 * @b: paremeter b
 * Description: a function that adds two integers and returns the result.
 * Return: return result of the output
 */

int add(int a, int b)
{
	result = a + b;
	return (result);
}
